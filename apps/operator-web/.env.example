# Database
DATABASE_URL=postgresql://questpie_autopilot_v2:replace-me-local-only@localhost:5432/questpie_autopilot_v2

# Application
APP_URL=http://localhost:3000
PORT=3000

# Auth (Better Auth)
BETTER_AUTH_SECRET=replace-with-at-least-32-random-bytes

# Email Adapter (console | smtp)
MAIL_ADAPTER=console

# SMTP (if using smtp adapter)
# SMTP_HOST=localhost
# SMTP_PORT=1025
